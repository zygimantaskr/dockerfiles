# 🐳 Dockerfiles Collection

A curated set of minimal, public Docker images for my portfolio and personal DevOps experiments.

Each folder contains:
- A single `Dockerfile`
- A minimal `README.md` describing its use
